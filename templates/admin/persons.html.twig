{% extends 'base.html.twig' %}
{% block javascripts %}
<script>
function setFilter(val) {
    document.querySelector('.filter-selection').setAttribute(`href`, `/admin/persons/` + val);
}
</script>

{% endblock %}

{% block body %}
    <!--
    <a href='{{ path('admin.persons') }}'><button class='btn btn-primary'>All</button></a>
    <a href='{{ path('admin.persons', {state: PERSON_STATE_ACTIVE}) }}'><button class='btn btn-primary'>Active</button></a>
    <a href='{{ path('admin.persons', {state: PERSON_STATE_BANNED}) }}'><button class='btn btn-primary'>Banned</button></a>
    <a href='{{ path('admin.persons', {state: PERSON_STATE_DELETED}) }}'><button class='btn btn-primary'>Deleted</button></a>
    --!>

    <select onchange=" setFilter(this.options[this.selectedIndex].value); ">
        <option value='0'>All</option>
        <option value='{{PERSON_STATE_ACTIVE}}'>Active</option>
        <option value='{{PERSON_STATE_BANNED}}'>Banned</option>
        <option value='{{PERSON_STATE_DELETED}}'>Deleted</option>
    </select>
    <a class='filter-selection' href='{{ path('admin.persons') }}'><button class='btn btn-primary'>Filter</button></a>
    <h1>
        {% if state == PERSON_STATE_ALL %}All
        {% elseif state == PERSON_STATE_ACTIVE %}Active
        {% elseif state == PERSON_STATE_BANNED %}Banned
        {% elseif state == PERSON_STATE_DELETED %}Deleted {% endif %}
        People:
    </h1>
    <table class='table'>
        <thead>
            <tr>
                <td>Login</td>
                <td>Last Name</td>
                <td>First Name</td>
                <td>Status</td>
                <td>Actions</td>
            </tr>
        </thead>
        <tbody>
            {% for person in persons %}
                <tr>
                    <td>{{person.login}}</td>
                    <td>{{person.lName}}</td>
                    <td>{{person.fName}}</td>
                    <td>
                        {%if person.state == PERSON_STATE_ACTIVE %}Active{% endif %}
                        {%if person.state == PERSON_STATE_BANNED %}Banned{% endif %}
                        {%if person.state == PERSON_STATE_DELETED %}Deleted{% endif %}
                    </td>
                    <td>
                        <a href="{{ path('admin.editPerson', { id: person.id }) }}"><button class='btn btn-success'>Edit</button></a>
                        {% if person.state == PERSON_STATE_DELETED %}
                            <a href="{{ path('admin.setPersonState', { id: person.id, state: PERSON_STATE_ACTIVE }) }}"><button class='btn btn-primary'>Undelete</button></a>
                        {% else %}
                                {% if person.state == PERSON_STATE_BANNED %}
                                    <a href="{{ path('admin.setPersonState', { id: person.id, state: PERSON_STATE_ACTIVE }) }}"><button class='btn btn-primary'>Unban</button></a>
                                {% else %}
                                    <a href="{{ path('admin.setPersonState', { id: person.id, state: PERSON_STATE_BANNED }) }}"><button class='btn btn-primary'>Ban</button></a>
                                {% endif %}
                            <a href="{{ path('admin.setPersonState', { id: person.id, state: PERSON_STATE_DELETED }) }}"><button class='btn btn-primary'>Delete</button></a>
                        {% endif %}
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    <table>
    <a href='{{ path('admin.addPerson') }}'><button class='btn btn-primary'>Add Person</button></a><br>
    <a href='{{ path('admin.panel') }}'><button class='btn btn-primary'>Panel</button></a><br>
    <a href='{{ path('app_logout') }}'><button class='btn btn-primary'>Logout</button></a>
{% endblock %}
